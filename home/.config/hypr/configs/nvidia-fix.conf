env = __GLX_VENDOR_LIBRARY_NAME,nvidia
# env = __NV_PRIME_RENDER_OFFLOAD,1 # Disabled globally to prevent dual-GPU terminal font texture corruption
# env = __VK_LAYER_NV_optimus,NVIDIA_only
# env = GBM_BACKEND,nvidia-drm # Disabled to prevent dual GPU buffer mismatch with Intel primary display
env = LIBVA_DRIVER_NAME,nvidia
env = NVD_BACKEND,direct

cursor {
    use_cpu_buffer = true
}
